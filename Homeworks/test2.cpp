#include<bits/stdc++.h>
using namespace std;

#define inf 1e11

long long add() {
    return inf;
}

int main() {
    long long a = inf, b = inf, c = inf, d = inf;
    cout << add()+add()+add()+add() << endl;
}
